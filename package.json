{
  "name": "autarkeia",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local server/local.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
